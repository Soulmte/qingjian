# Product

## Register

product

## Users

中文用户，把笔记当成长期资产的人：写代码的、写文档的、写论文与读书笔记的学生。

使用场景很具体：一台笔记本，白天在办公室或图书馆，深夜在只有屏幕光的房间里，一次坐两三个小时，屏幕上同时开着编辑器、浏览器和终端。他们要的是「把东西写下来」这一件事，不是管理一个知识库。

要完成的工作：打开一个本地文件夹，在里面写 `.md`，写完关掉。下次打开，东西还在，格式还是那样。中间不需要登录、不需要同步、不需要相信某个云。

## Product Purpose

青简是一个完全本地的所见即所得 Markdown 编辑器。

笔记是磁盘上普通的 `.md` 文件，数据库只承担元数据与检索索引。衡量成功的标准只有一条：用户会忘记这个数据库的存在，并且随手用别的编辑器打开那些文件时，看到的还是同一篇东西。

第二层目标是「像 Typora，但不要求用户交出文件」。所见即所得是主界面，源码是可切换的备选，不是反过来。

## Brand Personality

青简，竹简。三组词：**安静、克制、耐看**。

语气是一个不说话的文具：不解释自己，不提示「你好厉害」，不庆祝。空状态也只说明怎么开始，不卖萌。

情绪目标是「坐下来就能开始写」。界面不应该让人兴奋，也不应该让人分心 —— 理想状态是用户根本注意不到界面的存在。

## Anti-references

明确的坏例子：

- **VS Code**：蓝灰扁平、纯冷色、大面积单色块。工具感是对的，气质太冷、太「机柜」。
- **macOS 原生应用的半透明毛玻璃**：`backdrop-filter` 大面积铺开、无描边的浮层。在深色下不可控，且和「本地纸质笔记」这个隐喻冲突。
- **SaaS 落地页调性**：渐变文字、大数字配小标签、卡片网格、霓虹色。
- **Obsidian 的深紫黑**：色相强行绑定品牌，导致长时间阅读疲劳。
- **过度拟物**：纸张纹理、木纹、撕边、阴影堆叠。

## Design Principles

1. **纸是主角。** 屏幕上只有文档正文该被眼睛抓住。侧栏、工具栏、状态栏都是背景，任何一处的对比度都不该高过正文。
2. **颜色是状态，不是装饰。** 强调色出现在交互与状态上（当前项、焦点环、光标、选中、主操作、链接），不作为大面积的表面色铺开。给整块画布染色是过去的主要问题。
3. **换主题换的是气质，不是布局。** 切换明暗或强调色，不允许位移、不允许尺寸变化、不允许某个元素忽然显眼。
4. **长时间阅读优先。** 正文、次级文字都要满足 WCAG AA；宁可少一点「设计感」，不要让人在三小时后合上电脑眼睛发酸。
5. **暖调身份不能丢。** 六个强调色都建立在暖纸底上。借的是其他编辑器的亮度阶梯与对比纪律，不是它们的冷灰配色。

## Accessibility & Inclusion

- 正文与次级文字均须达到 WCAG AA（小字号 4.5:1，大字号 3:1）。次级文字当前只有 3.6~4.1，属于不达标，需要修。
- 不依赖色相单独传达状态：当前项除了底色还有左侧标记与字重，选中态除了描边还有形状。
- 尊重 `prefers-reduced-motion`。
- 六个强调色在明暗两种模式下亮度一致，因此色盲用户切换主题时，界面的层级关系不会改变。
