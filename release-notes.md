这一版把「检查更新」从「提示你去下载」换成了**静默自动更新**：下载、安装、重新打开，一气呵成。

**主要变化**

- **原地自动更新**：发现新版本时给出更新说明，点「立即更新」后青简会在后台下载。下载完成即可选择「立即重启并更新」，或者**直接关掉青简** —— 关闭时会自动把更新装上，下次打开就已是最新版。
- **全程无需操作**：不再弹出安装向导，也不再需要你去「下载」文件夹里手动双击安装包。安装包落在系统临时目录，装完由系统回收。
- **更新说明支持 Markdown**：新版本的更新说明在弹窗里按 Markdown 排版显示，标题、列表、行内代码都能看清，不再是挤成一团的纯文本。
- **更新包带签名校验**：每个安装包都由发布私钥签名，青简在安装前会验签；签名对不上就拒绝安装。静默安装意味着程序会替你执行安装包，这一步是底线。

**体验修复**

- 更新弹窗里关闭按钮的文案会随状态变化：下载完成后再点关闭就是「稍后（关闭时自动安装）」，让「关掉就会装」这件事一眼可见。
- 「关于」页与设置的检查更新入口统一走同一个更新器，不会再出现两套说法。

**升级方式**

这一版仍需要你手动装一次（因为它要装的是「以后能自动更新」的能力）。从 0.1.2 起，之后每次发版都会自动更新，无需再手动下载。直接运行安装包覆盖安装即可，工作区、设置与索引都在。
