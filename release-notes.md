这一版只改一件事，但大概是国内用户最想要的那件：**更新改成优先走 Gitee**。

**更新现在先问 Gitee，问不到再问 GitHub**

* **不用再等 GitHub 了。** GitHub 的安装包在国内经常只有几十 KB/s，或者干脆连不上；Gitee 上放的是同一份包，快得多。现在检查更新和下载都先走 Gitee，它不通才退到 GitHub——两边都试过才报失败。
* **签名校验一点没放松。** 从哪边下都验同一把钥匙，包被人换过一样装不上；换的只是下载的地方。
* **两边都慢的话**，可以在「设置 → 关于 → 更新」里填下载代理，或点「手动下载（Gitee）」自己去发布页下。

**从 0.1.9 或更早更新上来的话，有一步要说明**

更新地址是打包时写死在程序里的，所以**已经装上的旧版本还不认识 Gitee 这条路**，只会去问 GitHub。如果你现在检查更新很慢或一直失败，得先让它连通 GitHub 一次、装上这一版，之后就自动走 Gitee 了；实在连不上，去 Gitee 的发布页手动下这一版装上就行。

笔记、图片和历史版本都在原处，更新不会动它们。
