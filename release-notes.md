青简的第一个版本。

**写作**

- 所见即所得编辑，Markdown 语法在行内直接成形，不来回切预览
- 源码模式（Ctrl + /）随时对照原始 Markdown
- 专注模式（F8）只高亮当前段落、打字机模式（F9）光标所在行始终居中
- 表格、任务列表、代码高亮、公式（KaTeX 渲染）
- 大纲跳转、快速打开（Ctrl + P）、命令面板

**外观**

- 六套强调色（竹青 / 墨蓝 / 胭脂 / 赭石 / 黛紫 / 苍碧）× 明暗两式
- 界面字体与编辑器字体从系统已装字体里选，正文字号行高可调
- 六种代码底色

**导出**

- Word（.docx）：可分别为一至六级标题指定字体、字号、加粗与颜色，公式转成 Word 原生 OMML
- PDF（走 WebView2 静默打印，无页眉页脚）、HTML、纯文本
- 图片可等比例缩放、左中右对齐、可选图注

**图片**

- 默认存本地，也可上传到 GitHub / Gitee 图床，Markdown 里直接引用可公开访问的地址
- 剪贴板里的图片直接粘贴即可，应用内每张图可单个调整

**数据**

笔记正文始终以 `.md` 文件保存在你自己的目录里，SQLite 只记录元数据、标签与搜索索引。
卸载应用不会带走任何一篇笔记，也不锁定格式——任何编辑器都能打开。

**安装**

Windows 10/11 64 位。`setup.exe` 是单文件安装包，推荐；
`.msi` 供需要静默部署（`msiexec /i`）的场景。

---

已知限制：本版没有实现原地的静默自动更新。应用的「设置 → 更新」会检查新版本并
把安装包下载到「下载」文件夹，仍需要你关闭青简后自行运行安装包。
