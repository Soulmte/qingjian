这一版把「退回旧版本就再也打不开」那个坑填了，另外给安装包补上了发行者信息，并多了一份便携版。

**退回旧版本不会再毁掉数据库**

* 被更新版本的青简打开过笔记之后，再退回旧版本，旧版本会因为「库里有一条迁移它不认识」而拒绝启动。它把这种情况当成了坏库：改名搬走，再重建一个空的。
* 可搬走的不只是索引——设置、工作区列表也在里面，而那三样都不在 `.md` 文件里，重建不回来。现在这种情况会按旧的那一套把库正常打开，笔记、设置、工作区都在，同时提示你更新到最新版。真正坏掉的库照旧隔离重建。
* 顺带修掉一句会把人带偏的报错：旧版本报的是「另一个程序正在使用此文件」，听着像被别的东西占着，其实占用文件的是它自己。现在会把真正的第一个原因一并说清楚。

**安装包补上了发行者信息**

* 可执行文件里现在有发行者、版权、分类和描述（此前只有产品名和版本号）。
* 青简没有代码签名。在没有签名的情况下，这些信息能少给启发式一点把柄——浏览器和杀软对「既没签名、又没来历」的文件最不客气。

**多了一份便携版**

* Release 里多一个 `Qingjian_x.y.z_x64-portable.zip`，解压即用，不用安装。浏览器的下载保护主要盯 exe，zip 撞不上，算是一条绕开的路。便携版和安装版共用同一份设置与索引。
* 每一版还附一个 `SHA256SUMS.txt`，可以核对下载到的东西有没有被动过。
