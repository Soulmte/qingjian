这一版专治「把项目文件夹当工作区打开就卡死」，顺带把大文件的输入手感做顺了。

**修复**

- **不再扫整个项目，只扫笔记**：以前打开一个项目文件夹时，扫描会走遍每一层子目录——`node_modules` 里几千个包各自的 README 都被当成你的笔记收了进来，界面因此卡住。现在 `node_modules`、`target`、`dist`、`build`、`vendor`、`Pods` 这类构建与依赖目录会被整块跳过，只收录真正的 Markdown 笔记。
- **大文件打开与输入更跟手**：标题、大纲、字数统计原本每敲一个字都要把整篇文档重新过一遍；现在这些计算改为延后执行，编辑本身优先。另外超过 20 万字的文档会默认以源代码模式打开，上方给出一键切回所见即所得——想要富文本完全没问题，只是不再让第一次渲染把界面拖住。
- 读取文件时不再为了算内容哈希额外复制一份全文。
- 单个读不动的子目录（权限不足、网络盘掉线）不再让整次扫描失败，能读到的笔记照常列出。

**升级提示**

如果你之前已经把某个项目文件夹加成了工作区，升级后在工作区上执行一次「重新扫描」（或重新打开一次那个文件夹），之前混进来的第三方 README 就会被清掉。
