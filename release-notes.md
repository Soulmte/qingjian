这一版把配色重新做了一遍，并补上了文件夹管理。

**配色重做**

* **九个强调色**：竹青、墨蓝、胭脂、赭石、黛紫、苍碧、墨白、藤黄、IDEA 蓝。原来的六个偏灰，这一版把色度提到 sRGB 允许的上限。
* **不再整块画布染色。** 旧版把强调色以 14%~31% 混进每一块底色，混出来的既不够浓、又已经发脏，这正是「颜色沉闷」的来源。现在画布只沾 2% 以内，颜色出现在当前项、焦点环、主操作与链接上，需要它出场的地方反而更醒目。
* **深色模式真的变黑了。** 旧版画布的亮度比 IDEA 的面板还亮，侧栏的亮度直接等于 IDEA 的分隔线，所以看着是灰不是黑。现在阶梯重新排过：侧栏 → 工具栏 → 画布 → 纸面，最深一档比 IDEA 的编辑器底色还深。
* **切主题只换色相，不换明暗。** 九个强调色的亮度统一了，换色时画布的亮度不会再跟着浮动。
* 顺带修好三处看不到但晃眼睛的地方：浅色的次级文字对比度（3.98 → 4.69）、强调色文字压在自身淡底色上（九个色里有两个只有 4.18）、深色菜单里快捷键提示（4.31）。现在全部达到无障碍标准的 4.5。

**文件夹管理**

* 侧边栏右键可以**新建文件夹**；文件夹自己也能重命名、删除。删除会进系统回收站，之后仍可还原。
* 右键里有**在资源管理器打开**，直接定位到那个文件或文件夹。
* 删除文件夹前会先停掉待写的存档，不会出现「删掉了又被写回来」。

**侧边栏**

* 搜索框挪到独立一行。**搜索时不再把文件树顶掉**，而是把树收窄到匹配的笔记，所以还能看清它在哪个文件夹里；匹配到的正文片段放在那一行的悬浮提示里。
* 目录**默认折叠**；一个笔记都没有的文件夹不再显示（刚新建的会先留着）。
* 工具栏和右键菜单都补齐了文字和图标。
* 「刷新」与「从磁盘重新加载」在侧边栏顶部常驻一份，不用再找空白处右键。

**修复**

* **点任何操作会闪一下、然后切换失败。** 开发模式下 Vite 把笔记文件当成了项目文件，每存一次就硬刷新整个界面（正式版没有这个问题）。
* **在别处新建的 `.md` 看不到。** 打开工作区时改成从磁盘重新读，而不是读上一次的索引。
* 大文件不再降级成源码模式，改用 `content-visibility` 跳过屏幕外区块的排版，所见即所得照常。

**如果你在 0.1.5 或更早**

直接更新即可，笔记与设置都在原处。
