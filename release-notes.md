这一版把编辑区的快捷键修好了。

**快捷键**

* **Ctrl+1…6、Ctrl+0 按下去没有反应，现在都能用。** 这些键位在内部绑定失败后抛了异常，异常又没有人接住，所以按下去毫无动静，而设置里的快捷键说明照旧写着「可用」。受影响的共 21 条，标题与段落、插入表格（Ctrl+T）、插入链接（Ctrl+K）、代码块（Ctrl+Shift+K）、引用（Ctrl+Shift+Q）、有序与无序列表（Ctrl+Shift+[ / ]）都在其中。
* **用过工具栏之后，快捷键不再失灵。** 以前点一下工具栏，焦点会停在按钮上，编辑器就收不到键位了，必须先把光标点回正文；现在工具栏不再抢走焦点。
* **对齐键位分两种情形。** 光标在正文、标题或图片上时，Ctrl+Shift+E / Ctrl+Shift+R 对齐这一块；光标在表格里时对齐所在的列，顶栏的三个对齐按钮也会跟着显示当前列的对齐方式，不再变灰。
* 左对齐仍然没有默认键位（Ctrl+Shift+L 归了「显示 / 隐藏侧边栏」），请用顶栏按钮；设置 → 快捷键里写明了这一点。

**如果你在 0.1.5 或更早**

直接更新即可，笔记与设置都在原处。
