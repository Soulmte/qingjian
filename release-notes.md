这一版把安装包的收尾做干净了，也是**第一次可以通过自动更新送达的版本**。

**改进**

- **装完自动清理安装包**：自动更新下载的安装包原本会留在系统临时目录里。现在青简在启动时会顺手把它删掉——更新完重新打开时，临时目录里不会再多出安装包。
- **更新检查按钮恢复正常**：上一版里「设置 → 更新」的「检查更新」按钮一度被禁用，这一版修好了。

**如果你在用 0.1.2**

这一版会**静默送达**：启动青简后按提示更新即可，下载、安装、重启全自动，不需要你去下载文件夹找安装包。装完之后，这一版的安装包本身也会被自动清理。
