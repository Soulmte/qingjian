这一版加了三样新东西——历史版本、图片压缩、窗口记忆——也修掉了一个会悄悄改写笔记的老问题。

**笔记历史版本（新）**

* **覆盖之前自动留档，随时能回到过去。** 右键侧栏里的笔记，或点状态栏右下角的「历史版本」，会看到一条时间线：点一下看渲染出来的样子，点「恢复这一版」换回去。恢复错了还能再恢复回来。
* **留档是粗粒度的，这是故意的。** 同一篇相隔不足 5 分钟不重复记——自动保存是「停下来 0.8 秒就写一次」，逐次留档会让你敲几下就攒出一版，反而把真正有用的版本挤出上限。一次连续写作只留「动手之前」那一版。
* **「记一个版本」** 适合动手大改之前先钉一个：手动记下的不会被自动清理。自动版本保留最近 100 版。

**图片会先瘦身再存（新）**

* **粘贴或拖进来的图先缩尺寸、换格式再落盘。** 截图常见的 3000×2000 会缩到 1920 宽；BMP、TIFF、WebP 这些转成 JPEG。PNG 只缩尺寸不换格式，免得文字边上出现压缩噪声。
* 默认开启，可在「设置 → 图像 → 保存前压缩」里关掉，或改长边上限与 JPEG 质量。
* 压得明显时会说一句「已压缩 16.8 MB → 7.7 MB」，免得你以为存的是原图。

**窗口与打开方式**

* **双击 .md 不会再开出第二个青简。** 已经在运行时，新的一份会把文件交给已有窗口然后自己退出——此前是两个窗口各写一份，同一篇笔记可能互相覆盖。
* **窗口的大小和位置会记住了**，下次启动回到你离开时的样子。

**编辑器**

* **只是打开看一眼，不会再改写文件。** 一篇写法不在编辑器规范形里的笔记（分隔线写成 `---`、列表用 `*`、表格两侧留了空格），以前一打开就被按编辑器的写法重写一遍；现在不会了。
* 命令面板里几条命令补齐了图标（退出应用、查找与替换、插入图片 / 公式 / 任务列表等），九个强调色换成了色块预览。

**界面**

* **右侧大纲面板可以拖宽了**，双击分栏边框回到默认宽度；侧边栏也一样。
* **侧边栏装得下上千篇笔记了。** 文件树改成只渲染看得见的那几十行，展开几百个文件夹也不再卡。

**如果你在 0.1.8 或更早**

直接更新即可，笔记与设置都在原处。第一次启动会加一张历史版本表，一瞬的事。
