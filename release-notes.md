这一版把「双击 .md 就能用青简打开」补上了。

**新功能**

- **注册为 .md 打开方式**：安装包会把青简登记为 `.md` / `.markdown` 的处理程序，并写入打开命令。装好之后在资源管理器里右键任意 Markdown 文件 →「打开方式」就能看到青简，选一次「始终使用」即成为默认。
- **支持双击打开**：双击 `.md`（或把它拖到青简图标上）会直接打开那个文件——程序读取命令行里的文件路径，找到它、把所在文件夹加入工作区并选中它。此前这个参数是被忽略的，所以双击只会开一个空窗口。
- 从「打开方式」指过来的 `.txt` 也能打开。

**修复**

- 快捷键页把「左对齐」误标成可用：`Ctrl+Shift+L` 实际归「显示 / 隐藏侧边栏」，现在它正确显示为「待实现」并注明请用工具栏按钮。

**关于「成为默认」这件事**

Windows 10/11 不允许安装程序悄悄把某一类文件抢成默认，只能登记自己并出现在候选里。所以：

1. 装完这一版，青简会出现在 `.md` 的「打开方式」列表中。
2. 右键一个 `.md` →「打开方式」→「选择其他应用」→ 选青简 → 勾上「始终使用此应用」。
3. 或者到「设置 → 应用 → 默认应用」里按文件类型指定。

之后双击 `.md` 就直接打开青简了。

**升级方式**

直接运行新版安装包即可覆盖安装，工作区、设置与索引都在，不需要重新配置。若你已经在用 0.1.0，装完这一版后建议按上面的步骤把「始终使用」指到青简一次。
