这一版修掉了设置里几处「看着有、按下去没用」的地方，表格与更新也一并收拾了。

**设置**

* **行高滑杆现在真的会动。** 此前它被编辑器自带的一段样式盖住，拖动没有任何反应。
* **每一页都能单独恢复默认。** 「恢复本页默认」只清这一页，不再顺带把导出与图床的配置一起抹掉。想全清还是用「行为」页那一条，它现在会写明会清掉什么、不会清掉什么（访问令牌保留）。
* **滑杆旁边多了输入框。** 像 760 这种正卡在中间的值，可以直接输进去了；拖动也不再每动一下就写一次磁盘。
* **设置可以搜了。** 在左侧栏顶部输入关键词，例如「拼写」，点结果会直接跳到那一行并高亮一下。
* **导出页能预览了。** 改字号、行距、页边距时，上方会按当前设置实时渲染一份样例。

**表格**

* **源码不会再被重排。** 以前只要保存一次，每一行都会被按列宽补上一堆空格；现在存回去还是你写的样子。
* **表格的右键菜单补齐了**：上下左右插行列、移动行列、删除整张表格，光标在表格里就能用。

**侧边栏**

* **在别的编辑器里新增或删除的 Markdown 会自动出现**，不用再手动刷新。

**更新**

* **进度条改成真实进度。** 以前它一开始就有值、没跑满就跳到完成；现在按实际下载的字节数走，网络上没有进度可报时显示成流动状态，而不是编一个数字出来。
* **下载慢可以在「设置 → 更新」里填下载代理**，或者用页面上给出的 Gitee 镜像手动下载——同一份安装包。

**如果你在 0.1.7 或更早**

直接更新即可，笔记与设置都在原处。
