这一版修好一个很要命的问题：**窗口关不掉**。

**修复**

- **可以正常关闭了**：从 0.1.2 起，关窗改由界面自己接管（为了「关窗前顺手把更新装上」）。这条路径需要在权限清单里放开窗口销毁（`core:window:allow-destroy`），当时漏了这一条——于是窗口有响应、点 X 却没反应，只能去任务管理器强杀。这一版补上权限，并给关窗加了兜底：不管更新装成没装成，窗口最后一定会关掉。

**如果你正卡在 0.1.4**

不必先强杀。在青简里按 `Ctrl + ,` 打开设置 → 更新 →「检查更新」，或在启动时弹出的更新提示里点「立即更新」，下载完点「立即重启并更新」即可升级——这条路径不依赖关窗。
